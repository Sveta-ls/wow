{
  "name": "nextfirstapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "deploy": "npm run build && powershell -Command \"if (Test-Path 'out\\404') { Remove-Item -Path 'out\\404' -Recurse -Force }; New-Item -Path 'out\\.nojekyll' -ItemType File -Force\" && npx gh-pages -d out"
  },
  "dependencies": {
    "next": "16.0.6",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "babel-plugin-react-compiler": "1.0.0",
    "gh-pages": "^6.3.0"
  }
}
